package hackComputer.tests;

import hackComputer.CPU.CPU;
import org.junit.Assert;
import org.junit.Test;

public class CPUTest {
    @Test
    public void test() {
        CPU cpu = new CPU();
        boolean [] instruction, inM;

        instruction = program[i];
        inM = inMs[i];
        reset = resets[i];
        cpu.update

    }
}
